services:

  app:
    build: .

    container_name: todo-app

    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    networks:
      - app-network

networks:
  app-network:
    external: true
